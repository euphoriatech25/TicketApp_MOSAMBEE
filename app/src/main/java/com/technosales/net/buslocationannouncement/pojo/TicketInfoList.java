package com.technosales.net.buslocationannouncement.pojo;

public class TicketInfoList {
    public TicketInfoList() {

    }

    public String
            ticket_id,
            transactionType,
            transactionMedium,
            transactionAmount,
            transactionFee,
            transactionCommission,
            isOnline,
            offlineRefId,
            lat,
            lng,
            status,
            referenceId,
            referenceHash,
            device_time,
            userType,
            device_id,
            helper_id,
            passenger_id;


//            ticketNumber, ticketPrice, ticketType, ticketDate, ticketTime, ticketLat, ticketLng,card_id,mode,helper_id;
}
